# enter your credentials in the following format:

DB_HOST=your ip address
PORT=your port
USER_NAME=user name for the database
PASSWORD=password associated with the database username
DB_NAME=name of your database
JWT_SECRET=make up a secret key, which will be used to sign and verify your jwt tokens